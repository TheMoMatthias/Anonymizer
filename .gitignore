.claude/
.venv/
dist/
__pycache__/
*.pyc
.pytest_cache/
uv.lock.bak
tests/fixtures/generated/
# Vendored ML model packs (GLiNER ONNX, ~0.5-2 GB). Fetched on a connected
# machine and copied into the offline bundle by build_offline_bundle.ps1 --
# never committed.
vendor/
*.psd_report.json
config.local.yaml
